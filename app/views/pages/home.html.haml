%h1 Pages#home
%p Find me in app/views/pages/home.html.haml
First user using singular variable
= "#{@user.first_name} #{@user.last_name}"
%br
First user using all
= "#{@users.first.first_name} #{@users.first.last_name}"
